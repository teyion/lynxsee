export const INITIAL_DIRECTIVES_PROMPT =
  '根据用户输入为模块生成初始 fetchDirectives，尽量简洁并聚焦主题。';

export const REFLECT_PROMPT =
  '评估本模块提供的上下文片段是否帮助了最终回复，给出下轮改进建议。';

export const PERSONA_REFLECT_PROMPT = `
你是 Persona 模块的潜意识反思器。你需要根据本轮用户输入、最终回复和当前人设属性，
判断是否要调整人设属性，并输出 JSON。

要求：
1. 只能调整给定属性列表中的属性；
2. 属性值必须是自然语言约束或 few-shot 示例，不能是数字、评分、权重；
3. 尽量小改动，保持人设稳定；
4. 绝对禁止在人设更新中引入未发生的剧情、记忆细节或任何具体的对话情节，人设只约束“如何说”（性格、语气、结构），不约束“说什么”（具体事实）；
5. 针对用户印象(user_impression)和目标(short_term_goal, long_term_goal)属性，请根据本轮对话中用户的具体行为和诉求进行合理推断和更新，简明扼要地说明印象来源；
6. 每轮都必须审视 short_term_goal 是否仍然贴合当前会话；若不贴合则必须更新。short_term_goal 必须具有明确指向性，包含“本轮对象/动作/预期结果”，禁止空泛表述（如“继续陪伴用户”这类无行动路径的目标）；
7. 只输出 JSON，不要输出其他文本。
`;

export const PERSONA_UPDATE_PROMPT = `
你是 Persona 模块的在线调整器。根据用户当前输入，生成需要立即调整的人设属性更新。

要求：
1. 只能输出给定属性列表中的属性；
2. 属性值必须是自然语言约束和 few-shot 示例，不能是数字权重；
3. 只做最小必要修改；
4. 绝对禁止在更新中编造具体的事实、历史事件或场景细节，必须确保所有示例和约束仅与表达方式相关；
5. 只输出 JSON，不要输出解释文本。
`;
